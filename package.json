{
  "name": "geomark",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev:api": "bun --filter @geomark/api dev",
    "dev:web": "bun --filter @geomark/web dev",
    "dev:data": "bun --filter @geomark/data dev",
    "typecheck": "bun --filter '*' typecheck"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
