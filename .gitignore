node_modules/
dist/
.env
.env.*
!.env.example
!.env.prod.example
*.log
.DS_Store
# Root-anchored — match the host data-volume mount, not packages/data
/data/

# Generated SSR client chunks (rebuilt on every server start)
packages/web/_ssr/

# Tool caches + session state
.fallow/
.dex/
.claude/
